# Drop-in for getty@tty1 to auto-login the scribebox user
# Place in /etc/systemd/system/getty@tty1.service.d/autologin.conf

[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin scribebox --noclear %I $TERM
